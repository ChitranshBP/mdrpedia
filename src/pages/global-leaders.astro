---
import Layout from "../layouts/Layout.astro";
import { getCollection } from "astro:content";
import TierBadge from "../components/TierBadge.astro";
import { getSignedImage } from "../lib/image-security";

const doctors = await getCollection("doctors");
// Manual filter or tag-based for now.
// We'll filter by specific roles/affiliations or just specific slugs for the "Heads of Communities"
const leaders = doctors.filter((doc) =>
    [
        "Tedros Adhanom Ghebreyesus",
        "Anthony Fauci",
        "Atul Gawande",
        "Devi Shetty",
        "Patrick Soon-Shiong",
    ].includes(doc.data.fullName),
);
---

<Layout
    title="Global Health Leaders — MDRPedia"
    description="Heads of major international healthcare communities and organizations."
>
    <div class="container py-20">
        <header class="text-center mb-16">
            <div
                class="inline-block px-4 py-1 rounded-full bg-blue-500/10 text-blue-400 text-sm font-bold mb-4 border border-blue-500/20"
            >
                INTERNATIONAL LEADERSHIP
            </div>
            <h1 class="text-5xl font-bold text-white mb-6">
                Global Health Commanders
            </h1>
            <p class="text-xl text-gray-400 max-w-2xl mx-auto">
                The architects of global health policy, pandemic response, and
                international medical cooperation.
            </p>
        </header>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {
                leaders.map((doc) => (
                    <a
                        href={`/doctors/${doc.slug}`}
                        class="group relative flex flex-col bg-[#0f172a] border border-blue-500/20 rounded-2xl overflow-hidden hover:border-blue-400/50 transition-all duration-300 hover:-translate-y-1 hover:shadow-[0_0_30px_rgba(59,130,246,0.15)]"
                    >
                        <div class="aspect-[4/3] overflow-hidden">
                            {doc.data.portraitUrl ? (
                                <img
                                    src={getSignedImage(doc.data.portraitUrl, {
                                        width: 600,
                                        height: 450,
                                        gravity: "face",
                                        crop: "fill",
                                    })}
                                    alt={doc.data.fullName}
                                    class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105"
                                />
                            ) : (
                                <div class="w-full h-full bg-gray-800 flex items-center justify-center">
                                    <span class="text-4xl font-bold text-gray-500">
                                        {doc.data.fullName
                                            .split(" ")
                                            .map((w: string) => w[0])
                                            .slice(0, 2)
                                            .join("")
                                            .toUpperCase()}
                                    </span>
                                </div>
                            )}
                            {/* Blue Overlay Gradient */}
                            <div class="absolute inset-0 bg-gradient-to-t from-[#0f172a] to-transparent opacity-80" />
                        </div>

                        <div class="absolute top-4 right-4">
                            <TierBadge tier={doc.data.tier} size="sm" />
                        </div>

                        <div class="p-6 relative">
                            <div class="mb-4">
                                <span class="text-blue-400 text-xs font-bold tracking-wider uppercase border border-blue-500/30 px-2 py-1 rounded">
                                    {doc.data.affiliations?.[0]?.hospitalName ||
                                        "Global Leader"}
                                </span>
                            </div>
                            <h3 class="text-2xl font-bold text-white mb-2 group-hover:text-blue-400 transition-colors">
                                {doc.data.fullName}
                            </h3>
                            <p class="text-gray-400 mb-4 line-clamp-2">
                                {doc.data.bio}
                            </p>

                            <div class="flex items-center text-sm text-blue-400/80 font-medium">
                                View Profile{" "}
                                <span class="ml-2 group-hover:translate-x-1 transition-transform">
                                    →
                                </span>
                            </div>
                        </div>
                    </a>
                ))
            }
        </div>
    </div>
</Layout>
