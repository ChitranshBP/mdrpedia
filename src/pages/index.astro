---
/**
 * MDRPedia — Homepage
 * Clean, encyclopedia-style layout inspired by Wikipedia
 */

import Layout from "../layouts/Layout.astro";
import { getCollection } from "astro:content";

const allDoctors = await getCollection("doctors");
const totalCount = allDoctors.length;
const verifiedCount = allDoctors.filter(
	(d) => d.data.tier !== "UNRANKED",
).length;
const titanCount = allDoctors.filter((d) => d.data.tier === "TITAN").length;
const eliteCount = allDoctors.filter((d) => d.data.tier === "ELITE").length;
const countrySet = new Set(allDoctors.map((d) => d.data.geography.country));
const countriesCount = countrySet.size;

// Top specialties (filter out numeric values from bulk-import data)
const specMap = new Map<string, number>();
allDoctors.forEach((d) => {
	const s = d.data.specialty;
	if (s && s.length > 2 && isNaN(Number(String(s).replace(/,/g, "")))) {
		specMap.set(s, (specMap.get(s) || 0) + 1);
	}
});
const topSpecialties = [...specMap.entries()]
	.sort((a, b) => b[1] - a[1])
	.slice(0, 12)
	.map(([name, count]) => ({ name, count }));

// Build search index at build time
const searchIndex = allDoctors
	.filter(
		(d) =>
			d.data.fullName &&
			d.data.fullName.length > 2 &&
			isNaN(Number(d.data.fullName)),
	)
	.map((d) => ({
		id: d.id,
		n: d.data.fullName,
		s: d.data.specialty || "",
		t: d.data.tier || "UNRANKED",
		c: d.data.geography?.country || "",
	}))
	.slice(0, 800);
---

<Layout
	title="MDRPedia — The Free Encyclopedia of Verified Medical Professionals"
>
	<div class="wiki-home">
		<!-- Welcome Banner -->
		<div class="welcome-banner">
			<h1 class="welcome-title">
				Welcome to <strong>MDRPedia</strong>
			</h1>
			<p class="welcome-subtitle">
				The free, open registry of verified medical professionals
			</p>
		</div>

		<!-- Search -->
		<div class="search-section">
			<div class="search-container" id="mdr-search-root">
				<div class="search-input-wrap">
					<svg
						class="search-icon"
						viewBox="0 0 24 24"
						fill="none"
						stroke="currentColor"
						stroke-width="2"
					>
						<circle cx="11" cy="11" r="8"></circle>
						<path d="m21 21-4.3-4.3"></path>
					</svg>
					<input
						type="text"
						id="mdr-hero-search"
						class="search-input"
						placeholder="Search doctors by name, specialty, or country…"
						autocomplete="off"
						aria-label="Search MDRPedia"
					/>
					<div class="search-kbd"><kbd>⌘</kbd><kbd>K</kbd></div>
				</div>
				<div class="search-results" id="mdr-search-results" hidden>
				</div>
			</div>
			<div class="search-tags">
				<button class="search-tag" data-query="Neurosurgery"
					>Neurosurgery</button
				>
				<button class="search-tag" data-query="Cardiology"
					>Cardiology</button
				>
				<button class="search-tag" data-query="Oncology"
					>Oncology</button
				>
				<button class="search-tag" data-query="Immunology"
					>Immunology</button
				>
				<button class="search-tag" data-query="Transplant"
					>Transplant</button
				>
			</div>
		</div>

		<!-- Stats Line -->
		<p class="stats-line">
			<strong>{totalCount.toLocaleString()}</strong> verified profiles ·
			<strong>{titanCount}</strong> Titans ·
			<strong>{eliteCount}</strong> Elite ·
			<strong>{countriesCount}</strong> countries
		</p>

		<!-- Quick Navigation -->
		<div class="quick-nav">
			<a href="/doctors" class="quick-nav__card">
				<span class="quick-nav__icon">📋</span>
				<span class="quick-nav__label">Browse All</span>
				<span class="quick-nav__desc"
					>{totalCount.toLocaleString()} profiles</span
				>
			</a>
			<a href="/nobel-laureates" class="quick-nav__card">
				<span class="quick-nav__icon">🏅</span>
				<span class="quick-nav__label">Nobel Laureates</span>
				<span class="quick-nav__desc">Medicine & Physiology</span>
			</a>
			<a href="/global-leaders" class="quick-nav__card">
				<span class="quick-nav__icon">🌍</span>
				<span class="quick-nav__label">Global Leaders</span>
				<span class="quick-nav__desc">WHO, NIH & more</span>
			</a>
			<a href="/communities" class="quick-nav__card">
				<span class="quick-nav__icon">🏛️</span>
				<span class="quick-nav__label">Communities</span>
				<span class="quick-nav__desc">Organizations & specialties</span>
			</a>
			<a href="/compare" class="quick-nav__card">
				<span class="quick-nav__icon">⚖️</span>
				<span class="quick-nav__label">Compare</span>
				<span class="quick-nav__desc">Side-by-side metrics</span>
			</a>
			<a
				href="/doctor/claim"
				class="quick-nav__card quick-nav__card--accent"
			>
				<span class="quick-nav__icon">✋</span>
				<span class="quick-nav__label">Claim Profile</span>
				<span class="quick-nav__desc">Verify your identity</span>
			</a>
		</div>

		<!-- Specialties -->
		<section class="specialties-section">
			<h2 class="section-heading">Browse by Specialty</h2>
			<div class="specialties-grid">
				{
					topSpecialties.map((sp) => (
						<a
							href={`/doctors?q=${encodeURIComponent(sp.name)}`}
							class="specialty-chip"
						>
							<span class="specialty-chip__name">{sp.name}</span>
							<span class="specialty-chip__count">
								{sp.count}
							</span>
						</a>
					))
				}
			</div>
		</section>

		<!-- Tier System -->
		<section class="tiers-section">
			<h2 class="section-heading">Tier Classification</h2>
			<table class="tier-table">
				<thead>
					<tr>
						<th>Tier</th>
						<th>Criteria</th>
						<th>Profiles</th>
					</tr>
				</thead>
				<tbody>
					<tr>
						<td
							><span class="tier-label tier-label--titan"
								>🏆 Titan</span
							></td
						>
						<td>Top 0.01% — Paradigm shifters, H-index &gt; 60</td>
						<td>{titanCount}</td>
					</tr>
					<tr>
						<td
							><span class="tier-label tier-label--elite"
								>🥈 Elite</span
							></td
						>
						<td>Top 1% — National authorities, 100+ publications</td
						>
						<td>{eliteCount}</td>
					</tr>
					<tr>
						<td
							><span class="tier-label tier-label--master"
								>🥉 Master</span
							></td
						>
						<td>Top 3% — Regional mastery, 15+ years excellence</td>
						<td
							>{
								allDoctors.filter(
									(d) => d.data.tier === "MASTER",
								).length
							}</td
						>
					</tr>
				</tbody>
			</table>
		</section>

		<!-- About -->
		<section class="about-section">
			<h2 class="section-heading">About MDRPedia</h2>
			<p>
				MDRPedia is a free, open-access registry of medical
				professionals ranked by peer-reviewed research impact. Every
				profile is verified through DOI-linked citations, H-index
				scores, and institutional affiliations. The registry covers {
					countriesCount
				} countries and {topSpecialties.length}+ medical specialties.
			</p>
			<p>
				Profiles are classified into three tiers — Titan, Elite, and
				Master — based on verifiable metrics including publication
				count, citation impact, surgical volume, and peer recognition.
				All data is cross-referenced against PubMed, institutional
				records, and verified databases.
			</p>
		</section>
	</div>
</Layout>

<script define:vars={{ searchIndex }}>
	const DATA = searchIndex;
	const input = document.getElementById("mdr-hero-search");
	const resultsEl = document.getElementById("mdr-search-results");
	let activeIdx = -1;

	document.addEventListener("keydown", (e) => {
		if ((e.metaKey || e.ctrlKey) && e.key === "k") {
			e.preventDefault();
			input?.focus();
		}
	});

	function renderResults(matches) {
		if (!resultsEl) return;
		if (matches.length === 0) {
			resultsEl.innerHTML =
				'<div class="no-results">No matching doctors found</div>';
			resultsEl.hidden = false;
			return;
		}
		activeIdx = -1;
		resultsEl.innerHTML = matches
			.map((m, i) => {
				const tierClass =
					m.t === "TITAN"
						? "tier-titan"
						: m.t === "ELITE"
							? "tier-elite"
							: "tier-other";
				const tierLabel =
					m.t === "TITAN" ? "🏆" : m.t === "ELITE" ? "🥈" : "🥉";
				const initials = m.n
					.split(" ")
					.map((w) => w[0])
					.filter((_, j) => j < 2)
					.join("")
					.toUpperCase();
				return `<a href="/doctors/${m.id}" class="result-item ${tierClass}" data-idx="${i}">
				<span class="result-initials">${initials}</span>
				<span class="result-info">
					<span class="result-name">${m.n}</span>
					<span class="result-meta">${m.s}${m.c ? " · " + m.c : ""}</span>
				</span>
				<span class="result-tier">${tierLabel}</span>
			</a>`;
			})
			.join("");
		resultsEl.hidden = false;
	}

	let timer;
	input?.addEventListener("input", () => {
		clearTimeout(timer);
		timer = setTimeout(() => {
			const q = input.value.trim().toLowerCase();
			if (q.length < 2) {
				resultsEl && (resultsEl.hidden = true);
				return;
			}
			const matches = DATA.filter((d) =>
				`${d.n} ${d.s} ${d.c}`.toLowerCase().includes(q),
			).slice(0, 8);
			renderResults(matches);
		}, 150);
	});

	input?.addEventListener("keydown", (e) => {
		if (!resultsEl || resultsEl.hidden) return;
		const items = resultsEl.querySelectorAll(".result-item");
		if (e.key === "ArrowDown") {
			e.preventDefault();
			activeIdx = Math.min(activeIdx + 1, items.length - 1);
			items.forEach((el, i) =>
				el.classList.toggle("active", i === activeIdx),
			);
		} else if (e.key === "ArrowUp") {
			e.preventDefault();
			activeIdx = Math.max(activeIdx - 1, 0);
			items.forEach((el, i) =>
				el.classList.toggle("active", i === activeIdx),
			);
		} else if (e.key === "Enter" && activeIdx >= 0) {
			e.preventDefault();
			items[activeIdx]?.click();
		} else if (e.key === "Escape") {
			resultsEl.hidden = true;
		}
	});

	document.addEventListener("click", (e) => {
		if (!document.getElementById("mdr-search-root")?.contains(e.target)) {
			resultsEl && (resultsEl.hidden = true);
		}
	});

	document.querySelectorAll(".search-tag").forEach((btn) => {
		btn.addEventListener("click", () => {
			if (input) {
				input.value = btn.dataset.query;
				input.dispatchEvent(new Event("input"));
				input.focus();
			}
		});
	});
</script>

<style>
	/* ─── Wiki Home ──────────────────────────────────────────────────────── */
	.wiki-home {
		max-width: 900px;
		margin: 0 auto;
		padding: var(--space-xl) var(--space-lg);
	}

	/* Welcome */
	.welcome-banner {
		text-align: center;
		padding: var(--space-2xl) 0 var(--space-lg);
		border-bottom: 1px solid var(--border-subtle);
		margin-bottom: var(--space-xl);
	}
	.welcome-title {
		font-size: clamp(1.8rem, 4vw, 2.5rem);
		font-weight: 400;
		color: var(--text-primary);
		font-family: var(--font-serif);
	}
	.welcome-title strong {
		font-weight: 700;
	}
	.welcome-subtitle {
		margin-top: var(--space-xs);
		color: var(--text-secondary);
		font-size: 1.05rem;
	}

	/* Search */
	.search-section {
		margin-bottom: var(--space-lg);
	}
	.search-container {
		position: relative;
		max-width: 600px;
		margin: 0 auto;
	}
	.search-input-wrap {
		position: relative;
		display: flex;
		align-items: center;
	}
	.search-icon {
		position: absolute;
		left: 16px;
		width: 20px;
		height: 20px;
		color: var(--text-muted);
		pointer-events: none;
		z-index: 1;
	}
	.search-input {
		width: 100%;
		padding: 14px 80px 14px 46px;
		font-size: 1rem;
		font-family: var(--font-sans);
		background: rgba(255, 255, 255, 0.04);
		border: 1px solid var(--border-subtle);
		border-radius: 8px;
		color: var(--text-primary);
		outline: none;
		transition: border-color 0.2s;
	}
	.search-input::placeholder {
		color: var(--text-muted);
	}
	.search-input:focus {
		border-color: var(--accent-purple);
		box-shadow: 0 0 0 3px rgba(139, 92, 246, 0.1);
	}
	.search-kbd {
		position: absolute;
		right: 14px;
		display: flex;
		gap: 3px;
		pointer-events: none;
	}
	.search-kbd kbd {
		font-size: 0.65rem;
		font-weight: 600;
		padding: 2px 6px;
		background: rgba(255, 255, 255, 0.06);
		border: 1px solid rgba(255, 255, 255, 0.12);
		border-radius: 4px;
		color: var(--text-muted);
		font-family: var(--font-sans);
	}
	.search-tags {
		display: flex;
		flex-wrap: wrap;
		gap: 6px;
		margin-top: 10px;
		justify-content: center;
	}
	.search-tag {
		font-size: 0.78rem;
		padding: 4px 12px;
		background: transparent;
		border: 1px solid var(--border-subtle);
		border-radius: 4px;
		color: var(--text-secondary);
		cursor: pointer;
		font-family: var(--font-sans);
		transition: all 0.15s;
	}
	.search-tag:hover {
		border-color: var(--accent-purple);
		color: var(--accent-purple);
	}

	/* Results */
	.search-results {
		position: absolute;
		z-index: 100;
		top: calc(100% + 6px);
		width: 100%;
		background: var(--bg-card, #16162a);
		border: 1px solid var(--border-subtle);
		border-radius: 8px;
		box-shadow: 0 12px 40px rgba(0, 0, 0, 0.4);
		overflow: hidden;
		max-height: 380px;
		overflow-y: auto;
	}
	.result-item {
		display: flex;
		align-items: center;
		gap: 12px;
		padding: 10px 16px;
		text-decoration: none;
		color: var(--text-primary);
		transition: background 0.1s;
		border-bottom: 1px solid rgba(255, 255, 255, 0.03);
	}
	.result-item:hover,
	.result-item.active {
		background: rgba(139, 92, 246, 0.08);
	}
	.result-initials {
		width: 36px;
		height: 36px;
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		font-weight: 700;
		font-size: 0.78rem;
		flex-shrink: 0;
		background: rgba(255, 255, 255, 0.06);
		color: var(--text-secondary);
		border: 1px solid var(--border-subtle);
	}
	.tier-titan .result-initials {
		background: rgba(255, 215, 0, 0.1);
		color: #ffd700;
		border-color: rgba(255, 215, 0, 0.3);
	}
	.tier-elite .result-initials {
		background: rgba(0, 170, 255, 0.08);
		color: #00aaff;
		border-color: rgba(0, 170, 255, 0.2);
	}
	.result-info {
		flex: 1;
		min-width: 0;
		display: flex;
		flex-direction: column;
	}
	.result-name {
		font-weight: 600;
		font-size: 0.9rem;
		white-space: nowrap;
		overflow: hidden;
		text-overflow: ellipsis;
	}
	.tier-titan .result-name {
		color: #ffd700;
	}
	.result-meta {
		font-size: 0.78rem;
		color: var(--text-muted);
		white-space: nowrap;
		overflow: hidden;
		text-overflow: ellipsis;
	}
	.result-tier {
		font-size: 1rem;
		flex-shrink: 0;
	}
	.no-results {
		padding: 16px;
		text-align: center;
		color: var(--text-muted);
		font-size: 0.88rem;
	}

	/* Stats Line */
	.stats-line {
		text-align: center;
		color: var(--text-secondary);
		font-size: 0.9rem;
		margin-bottom: var(--space-xl);
	}
	.stats-line strong {
		color: var(--text-primary);
	}

	/* Quick Nav */
	.quick-nav {
		display: grid;
		grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
		gap: 12px;
		margin-bottom: var(--space-2xl);
	}
	.quick-nav__card {
		display: flex;
		flex-direction: column;
		padding: 16px;
		background: rgba(255, 255, 255, 0.03);
		border: 1px solid var(--border-subtle);
		border-radius: 8px;
		text-decoration: none;
		color: var(--text-primary);
		transition: all 0.15s;
	}
	.quick-nav__card:hover {
		border-color: var(--accent-purple);
		background: rgba(139, 92, 246, 0.05);
	}
	.quick-nav__card--accent {
		border-color: rgba(139, 92, 246, 0.3);
	}
	.quick-nav__icon {
		font-size: 1.4rem;
		margin-bottom: 8px;
	}
	.quick-nav__label {
		font-weight: 600;
		font-size: 0.9rem;
	}
	.quick-nav__desc {
		font-size: 0.75rem;
		color: var(--text-muted);
		margin-top: 2px;
	}

	/* Section */
	.section-heading {
		font-size: 1.25rem;
		font-weight: 700;
		color: var(--text-primary);
		margin-bottom: var(--space-md);
		padding-bottom: var(--space-xs);
		border-bottom: 1px solid var(--border-subtle);
	}

	/* Specialties */
	.specialties-section {
		margin-bottom: var(--space-2xl);
	}
	.specialties-grid {
		display: flex;
		flex-wrap: wrap;
		gap: 8px;
	}
	.specialty-chip {
		display: inline-flex;
		align-items: center;
		gap: 6px;
		padding: 6px 14px;
		background: rgba(255, 255, 255, 0.03);
		border: 1px solid var(--border-subtle);
		border-radius: 4px;
		text-decoration: none;
		color: var(--text-secondary);
		font-size: 0.85rem;
		transition: all 0.15s;
	}
	.specialty-chip:hover {
		border-color: var(--accent-purple);
		color: var(--accent-purple);
	}
	.specialty-chip__count {
		font-size: 0.7rem;
		background: rgba(255, 255, 255, 0.06);
		padding: 1px 6px;
		border-radius: 3px;
		color: var(--text-muted);
	}

	/* Tier Table */
	.tiers-section {
		margin-bottom: var(--space-2xl);
	}
	.tier-table {
		width: 100%;
		border-collapse: collapse;
		font-size: 0.9rem;
	}
	.tier-table th {
		text-align: left;
		padding: 10px 12px;
		border-bottom: 2px solid var(--border-subtle);
		color: var(--text-muted);
		font-size: 0.78rem;
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 0.05em;
	}
	.tier-table td {
		padding: 10px 12px;
		border-bottom: 1px solid rgba(255, 255, 255, 0.04);
		color: var(--text-secondary);
	}
	.tier-table tr:hover td {
		background: rgba(255, 255, 255, 0.02);
	}
	.tier-label {
		font-weight: 600;
		padding: 3px 10px;
		border-radius: 4px;
		font-size: 0.82rem;
		white-space: nowrap;
	}
	.tier-label--titan {
		background: rgba(255, 215, 0, 0.1);
		color: #ffd700;
	}
	.tier-label--elite {
		background: rgba(0, 170, 255, 0.08);
		color: #00aaff;
	}
	.tier-label--master {
		background: rgba(0, 204, 102, 0.08);
		color: #00cc66;
	}

	/* About */
	.about-section {
		margin-bottom: var(--space-2xl);
	}
	.about-section p {
		color: var(--text-secondary);
		font-size: 0.92rem;
		line-height: 1.7;
		margin-bottom: var(--space-sm);
	}

	/* Responsive */
	@media (max-width: 600px) {
		.wiki-home {
			padding: var(--space-md);
		}
		.quick-nav {
			grid-template-columns: repeat(2, 1fr);
		}
		.tier-table {
			font-size: 0.8rem;
		}
	}
</style>
