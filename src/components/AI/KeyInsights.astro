---
/**
 * KeyInsights - Bullet Summary of Doctor's Profile
 * Quick takeaways for AI and readers
 */

import type { KeyInsightsSummary, KeyInsight } from '../../lib/ai-content-types';

interface Props {
    insights: KeyInsightsSummary;
    doctorName: string;
    tier?: string;
}

const { insights, doctorName, tier = 'UNRANKED' } = Astro.props;

// Category configuration
const categoryConfig: Record<KeyInsight['category'], { icon: string; color: string }> = {
    ACHIEVEMENT: { icon: '<svg class="ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="16" height="16"><path d="M6 9H4.5a2.5 2.5 0 0 1 0-5H6m12 5h1.5a2.5 2.5 0 0 0 0-5H18M6 9a6 6 0 0 0 12 0V4H6v5zm3 12h6m-3-3v3"/></svg>', color: '#ffd700' },
    INNOVATION: { icon: '<svg class="ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="16" height="16"><path d="M9 21h6m-3-3v3M12 3a6 6 0 0 0-4 10.5V17h8v-3.5A6 6 0 0 0 12 3z"/></svg>', color: '#f59e0b' },
    IMPACT: { icon: '<svg class="ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="16" height="16"><path d="M12 22c5.523 0 10-4.477 10-10S17.523 2 12 2 2 6.477 2 12s4.477 10 10 10zM2 12h20M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg>', color: '#10b981' },
    EXPERTISE: { icon: '<svg class="ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="16" height="16"><path d="M12 22c5.523 0 10-4.477 10-10S17.523 2 12 2 2 6.477 2 12s4.477 10 10 10zM12 18a6 6 0 1 0 0-12 6 6 0 0 0 0 12zM12 14a2 2 0 1 0 0-4 2 2 0 0 0 0 4z"/></svg>', color: '#3b82f6' },
    RECOGNITION: { icon: '<svg class="ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="16" height="16"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>', color: '#ec4899' },
    LEADERSHIP: { icon: '<svg class="ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="16" height="16"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>', color: '#8b5cf6' },
};
---

<section class={`insights-section tier-${tier.toLowerCase()}`}>
    <div class="section-header">
        <h2 class="section-title">
            <svg width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/>
            </svg>
            Key Insights
        </h2>
        {insights.lastUpdated && (
            <span class="updated-badge">Updated {insights.lastUpdated}</span>
        )}
    </div>

    <!-- One-liner -->
    <div class="oneliner-card">
        <blockquote class="oneliner-text">{insights.oneLiner}</blockquote>
    </div>

    <!-- Quick Stats -->
    {insights.quickStats.length > 0 && (
        <div class="stats-row">
            {insights.quickStats.map(stat => (
                <div class="stat-card">
                    {stat.icon && <span class="stat-icon">{stat.icon}</span>}
                    <span class="stat-value">{stat.value}</span>
                    <span class="stat-label">{stat.label}</span>
                </div>
            ))}
        </div>
    )}

    <!-- Notable For -->
    {insights.notableFor.length > 0 && (
        <div class="notable-section">
            <h3 class="notable-title">Known For</h3>
            <div class="notable-tags">
                {insights.notableFor.map(item => (
                    <span class="notable-tag">{item}</span>
                ))}
            </div>
        </div>
    )}

    <!-- Top Insights -->
    <div class="insights-list">
        {insights.topInsights.map((insight, index) => (
            <div class="insight-card" style={`--insight-color: ${categoryConfig[insight.category].color};`}>
                <div class="insight-header">
                    <span class="insight-icon" set:html={categoryConfig[insight.category].icon}></span>
                    <span class="insight-category">{insight.category.replace('_', ' ')}</span>
                </div>
                <p class="insight-text">{insight.insight}</p>
                {insight.metric && (
                    <div class="insight-metric">
                        <span class="metric-value">{insight.metric.value}</span>
                        <span class="metric-label">{insight.metric.label}</span>
                    </div>
                )}
                {insight.evidence && (
                    <p class="insight-evidence">{insight.evidence}</p>
                )}
                {insight.source && (
                    <a
                        href={insight.source.url || (insight.source.type === 'DOI' ? `https://doi.org/${insight.source.identifier}` : '#')}
                        target="_blank"
                        rel="noopener"
                        class="insight-source"
                    >
                        {insight.source.type}: {insight.source.identifier}
                    </a>
                )}
            </div>
        ))}
    </div>

    <!-- AI-Generated Summary -->
    {insights.aiGeneratedSummary && (
        <div class="ai-summary">
            <div class="ai-summary-header">
                <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                    <path d="M12 8V4H8"/>
                    <rect x="4" y="4" width="16" height="16" rx="2"/>
                    <path d="M12 8l-4 4"/>
                    <path d="M12 8l4 4"/>
                    <path d="M8 16l4-4 4 4"/>
                </svg>
                <span>AI-Generated Summary</span>
            </div>
            <p class="ai-summary-text">{insights.aiGeneratedSummary}</p>
        </div>
    )}

    <!-- Hidden semantic layer for AI -->
    <div class="ai-semantic-layer" aria-hidden="true" style="display:none;">
        <h3>Key Insights for {doctorName}</h3>
        <p>Summary: {insights.oneLiner}</p>
        <h4>Quick Facts:</h4>
        <ul>
            {insights.quickStats.map(s => <li>{s.label}: {s.value}</li>)}
        </ul>
        <h4>Notable for:</h4>
        <p>{insights.notableFor.join(', ')}</p>
        <h4>Key Insights:</h4>
        <ul>
            {insights.topInsights.map(i => (
                <li>[{i.category}] {i.insight} {i.metric ? `(${i.metric.value} ${i.metric.label})` : ''}</li>
            ))}
        </ul>
    </div>
</section>

<style>
    .insights-section {
        background: var(--paper-2);
        border: 1px solid var(--rule);
        border-radius: var(--r-4);
        padding: 2rem;
        margin-bottom: 2rem;
        
    }

    .section-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 1rem;
        margin-bottom: 1.5rem;
        padding-bottom: 1rem;
        border-bottom: 1px solid var(--rule);
    }

    .section-title {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        font-size: 1.25rem;
        font-weight: 500;
        margin: 0;
        color: var(--ink);
    }

    .section-title svg {
        color: var(--aged-gold);
        opacity: 0.9;
    }

    .updated-badge {
        font-size: 0.7rem;
        color: var(--ink-3);
    }

    /* One-liner */
    .oneliner-card {
        margin-bottom: 1.5rem;
        padding: 1.25rem;
        background: var(--paper-3);
        border: 1px solid var(--rule);
        border-radius: var(--r-4);
    }

    .oneliner-text {
        margin: 0;
        font-size: 1.1rem;
        font-weight: 500;
        color: var(--ink);
        line-height: 1.6;
        text-align: center;
    }

    /* Stats Row */
    .stats-row {
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
        margin-bottom: 1.5rem;
    }

    .stat-card {
        flex: 1;
        min-width: 100px;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 0.25rem;
        padding: 1rem;
        background: var(--paper-2);
        border: 1px solid var(--rule);
        border-radius: var(--r-3);
        text-align: center;
    }

    .stat-icon {
        font-size: 1.25rem;
    }

    .stat-value {
        font-size: 1.5rem;
        font-weight: 700;
        color: var(--ink);
    }

    .stat-label {
        font-size: 0.7rem;
        color: var(--ink-3);
    }

    /* Notable For */
    .notable-section {
        margin-bottom: 1.5rem;
    }

    .notable-title {
        font-size: 0.85rem;
        font-weight: 600;
        margin: 0 0 0.75rem;
        color: var(--ink-3);
    }

    .notable-tags {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
    }

    .notable-tag {
        padding: 6px 14px;
        background: var(--paper-3);
        border: 1px solid var(--rule);
        border-radius: var(--r-pill);
        font-size: 0.85rem;
        color: var(--verdant);
    }

    /* Insights List */
    .insights-list {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
        gap: 1rem;
        margin-bottom: 1.5rem;
    }

    .insight-card {
        padding: 1.25rem;
        background: var(--paper-2);
        border: 1px solid var(--rule);
        border-left: 3px solid var(--insight-color);
        border-radius: var(--r-3);
        transition: all 0.2s ease;
    }

    .insight-card:hover {
        background: var(--paper-3);
        border-color: var(--rule-strong);
    }

    .insight-header {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        margin-bottom: 0.75rem;
    }

    .insight-icon {
        font-size: 1.1rem;
    }

    .insight-category {
        font-size: 0.65rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        color: var(--insight-color);
    }

    .insight-text {
        font-size: 0.95rem;
        color: var(--ink-2);
        line-height: 1.6;
        margin: 0 0 0.75rem;
    }

    .insight-metric {
        display: inline-flex;
        align-items: baseline;
        gap: 0.35rem;
        padding: 4px 10px;
        background: color-mix(in srgb, var(--insight-color) 15%, transparent);
        border-radius: 6px;
        margin-bottom: 0.5rem;
    }

    .metric-value {
        font-size: 1rem;
        font-weight: 700;
        color: var(--insight-color);
    }

    .metric-label {
        font-size: 0.75rem;
        color: var(--ink-3);
    }

    .insight-evidence {
        font-size: 0.8rem;
        font-style: italic;
        color: var(--ink-3);
        line-height: 1.5;
        margin: 0.5rem 0;
    }

    .insight-source {
        display: inline-block;
        padding: 2px 6px;
        background: var(--paper-3);
        border-radius: 4px;
        font-size: 0.7rem;
        color: var(--ink-3);
        text-decoration: none;
        transition: all 0.2s;
    }

    .insight-source:hover {
        background: var(--paper-3);
        color: var(--ink-blue);
    }

    /* AI Summary */
    .ai-summary {
        padding: 1.25rem;
        background: var(--paper-3);
        border: 1px solid var(--rule);
        border-radius: var(--r-4);
    }

    .ai-summary-header {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        margin-bottom: 0.75rem;
        font-size: 0.75rem;
        font-weight: 600;
        color: var(--ink-blue);
        text-transform: uppercase;
        letter-spacing: 0.03em;
    }

    .ai-summary-header svg {
        color: var(--ink-blue);
    }

    .ai-summary-text {
        font-size: 0.9rem;
        color: var(--ink-2);
        line-height: 1.7;
        margin: 0;
    }

    /* Tier colors */
    .tier-titan .section-title svg { color: var(--aged-gold); }
    .tier-elite .section-title svg { color: var(--ink-blue); }
    .tier-master .section-title svg { color: var(--verdant); }

    @media (max-width: 768px) {
        .insights-section {
            padding: 1.5rem;
        }

        .stats-row {
            flex-direction: column;
        }

        .stat-card {
            flex-direction: row;
            justify-content: center;
            gap: 0.75rem;
        }

        .insights-list {
            grid-template-columns: 1fr;
        }
    }
</style>
