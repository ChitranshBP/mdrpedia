---
/**
 * MDRPedia — Profile Embed Component
 * Easy URL sharing and embed code generation for doctor profiles
 */

interface Props {
    slug: string;
    fullName: string;
    specialty: string;
    tier: string;
    hasImage: boolean;
    totalImpact?: number;
    hIndex?: number;
    livesSaved?: number;
}

const { slug, fullName, specialty, tier, hasImage, totalImpact = 0, hIndex = 0, livesSaved = 0 } = Astro.props;

const baseUrl = Astro.site?.toString().replace(/\/$/, '') || 'https://mdrpedia.com';
const profileUrl = `${baseUrl}/doctors/${slug}`;
const tierColor = tier === 'TITAN' ? '#ffd700' : tier === 'ELITE' ? '#00aaff' : tier === 'MASTER' ? '#00cc66' : '#888';
const tierLabel = tier === 'TITAN' ? 'Top 0.01%' : tier === 'ELITE' ? 'Top 1%' : tier === 'MASTER' ? 'Top 3%' : '';

// Simple embed badge
const embedCode = `<a href="${profileUrl}" target="_blank" rel="noopener" style="display:inline-flex;align-items:center;gap:8px;padding:8px 16px;background:#1a1a2e;border:1px solid rgba(255,255,255,0.1);border-radius:8px;text-decoration:none;color:#fff;font-family:system-ui,sans-serif;font-size:14px;"><span style="color:${tierColor};">●</span>${fullName} — MDRPedia</a>`;

// Rich embed with impact metrics
const richEmbedCode = `<div style="display:inline-block;font-family:system-ui,sans-serif;background:linear-gradient(135deg,#1a1a2e,#0d0d1a);border:1px solid ${tierColor}40;border-radius:12px;padding:16px 20px;min-width:280px;text-decoration:none;"><a href="${profileUrl}" target="_blank" rel="noopener" style="text-decoration:none;"><div style="display:flex;align-items:center;gap:12px;margin-bottom:12px;"><span style="width:12px;height:12px;border-radius:50%;background:${tierColor};box-shadow:0 0 10px ${tierColor}80;"></span><span style="color:#fff;font-weight:700;font-size:16px;">${fullName}</span></div><div style="color:#888;font-size:12px;margin-bottom:12px;">${specialty}${tierLabel ? ` · ${tierLabel}` : ''}</div>${totalImpact > 0 ? `<div style="display:flex;gap:16px;"><div style="text-align:center;"><div style="color:${tierColor};font-weight:800;font-size:20px;">${totalImpact.toLocaleString()}</div><div style="color:#666;font-size:10px;text-transform:uppercase;">Impact</div></div>${hIndex > 0 ? `<div style="text-align:center;"><div style="color:#a78bfa;font-weight:800;font-size:20px;">${hIndex}</div><div style="color:#666;font-size:10px;text-transform:uppercase;">H-Index</div></div>` : ''}${livesSaved > 0 ? `<div style="text-align:center;"><div style="color:#10b981;font-weight:800;font-size:20px;">${livesSaved.toLocaleString()}</div><div style="color:#666;font-size:10px;text-transform:uppercase;">Lives Saved</div></div>` : ''}</div>` : ''}<div style="margin-top:12px;padding-top:8px;border-top:1px solid rgba(255,255,255,0.1);font-size:10px;color:#666;">Verified on MDRPedia</div></a></div>`;
---

<div class="embed-widget">
    <button class="embed-toggle" id="embed-toggle" aria-expanded="false">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/>
            <path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/>
        </svg>
        Share Profile
    </button>

    <div class="embed-panel" id="embed-panel" hidden>
        <div class="embed-backdrop" id="embed-backdrop"></div>
        <div class="embed-dialog">
        <div class="embed-header">
            <h4>Share or Embed Profile</h4>
            <button class="close-btn" id="embed-close" aria-label="Close">×</button>
        </div>

        <!-- Profile URL -->
        <div class="embed-section">
            <label>Profile URL</label>
            <div class="input-group">
                <input type="text" value={profileUrl} readonly id="profile-url" />
                <button class="copy-btn" data-copy="profile-url" title="Copy URL">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <rect x="9" y="9" width="13" height="13" rx="2"/>
                        <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/>
                    </svg>
                </button>
            </div>
        </div>

        <!-- Quick Share Buttons -->
        <div class="embed-section">
            <label>Quick Share</label>
            <div class="share-buttons">
                <a
                    href={`https://twitter.com/intent/tweet?text=${encodeURIComponent(`${fullName} — ${specialty} | MDRPedia`)}&url=${encodeURIComponent(profileUrl)}`}
                    target="_blank"
                    rel="noopener"
                    class="share-btn twitter"
                    title="Share on X (Twitter)"
                >
                    𝕏
                </a>
                <a
                    href={`https://www.linkedin.com/sharing/share-offsite/?url=${encodeURIComponent(profileUrl)}`}
                    target="_blank"
                    rel="noopener"
                    class="share-btn linkedin"
                    title="Share on LinkedIn"
                >
                    in
                </a>
                <a
                    href={`mailto:?subject=${encodeURIComponent(`${fullName} — MDRPedia`)}&body=${encodeURIComponent(`Check out ${fullName}'s profile on MDRPedia: ${profileUrl}`)}`}
                    class="share-btn email"
                    title="Share via Email"
                >
                    ✉
                </a>
                <a
                    href={`https://wa.me/?text=${encodeURIComponent(`${fullName} — ${specialty} | MDRPedia ${profileUrl}`)}`}
                    target="_blank"
                    rel="noopener"
                    class="share-btn whatsapp"
                    title="Share on WhatsApp"
                >
                    ●
                </a>
            </div>
        </div>

        <!-- Embed Options -->
        <div class="embed-section">
            <label>Embed Style</label>
            <div class="embed-tabs">
                <button class="embed-tab active" data-tab="simple">Simple Badge</button>
                <button class="embed-tab" data-tab="rich">Rich Card</button>
            </div>
        </div>

        <!-- Simple Embed Code -->
        <div class="embed-section embed-content" id="embed-simple">
            <p class="embed-hint">Minimal link badge for your website</p>
            <div class="input-group">
                <textarea readonly id="embed-code" rows="3">{embedCode}</textarea>
                <button class="copy-btn" data-copy="embed-code" title="Copy Embed Code">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <rect x="9" y="9" width="13" height="13" rx="2"/>
                        <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/>
                    </svg>
                </button>
            </div>
            <div class="embed-preview">
                <span class="preview-label">Preview:</span>
                <div class="preview-badge" style={`--tier-color: ${tierColor}`}>
                    <span class="badge-dot">●</span>
                    {fullName} — MDRPedia
                </div>
            </div>
        </div>

        <!-- Rich Embed Code with Impact Metrics -->
        <div class="embed-section embed-content" id="embed-rich" hidden>
            <p class="embed-hint">Impact card with key metrics</p>
            <div class="input-group">
                <textarea readonly id="embed-code-rich" rows="6">{richEmbedCode}</textarea>
                <button class="copy-btn" data-copy="embed-code-rich" title="Copy Rich Embed">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <rect x="9" y="9" width="13" height="13" rx="2"/>
                        <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/>
                    </svg>
                </button>
            </div>
            <div class="embed-preview">
                <span class="preview-label">Preview:</span>
                <div class="preview-card" style={`--tier-color: ${tierColor}`}>
                    <div class="preview-card__header">
                        <span class="preview-card__dot"></span>
                        <span class="preview-card__name">{fullName}</span>
                    </div>
                    <div class="preview-card__specialty">{specialty}{tierLabel && ` · ${tierLabel}`}</div>
                    {totalImpact > 0 && (
                        <div class="preview-card__metrics">
                            <div class="preview-metric">
                                <span class="preview-metric__value" style="color: var(--tier-color)">{totalImpact.toLocaleString()}</span>
                                <span class="preview-metric__label">Impact</span>
                            </div>
                            {hIndex > 0 && (
                                <div class="preview-metric">
                                    <span class="preview-metric__value" style="color: #a78bfa">{hIndex}</span>
                                    <span class="preview-metric__label">H-Index</span>
                                </div>
                            )}
                            {livesSaved > 0 && (
                                <div class="preview-metric">
                                    <span class="preview-metric__value" style="color: #10b981">{livesSaved.toLocaleString()}</span>
                                    <span class="preview-metric__label">Lives Saved</span>
                                </div>
                            )}
                        </div>
                    )}
                    <div class="preview-card__footer">Verified on MDRPedia</div>
                </div>
            </div>
        </div>

        <!-- Recommend Profile (when no image) -->
        {!hasImage && (
            <div class="embed-section recommend-section">
                <label>Help Improve This Profile</label>
                <p class="embed-hint">This profile doesn't have a photo. Know this physician? Help us add one.</p>
                <a href={`/suggest-edit?profile=${slug}&type=image`} class="recommend-btn">
                    Suggest a Photo
                </a>
            </div>
        )}

        <!-- Copied Toast -->
        <div class="copied-toast" id="copied-toast">Copied!</div>
        </div><!-- .embed-dialog -->
    </div>
</div>

<script>
    const toggle = document.getElementById('embed-toggle');
    const panel = document.getElementById('embed-panel');
    const backdrop = document.getElementById('embed-backdrop');
    const closeBtn = document.getElementById('embed-close');
    const copyBtns = document.querySelectorAll('.embed-widget .copy-btn');
    const toast = document.getElementById('copied-toast');
    const embedTabs = document.querySelectorAll('.embed-tab');
    const embedSimple = document.getElementById('embed-simple');
    const embedRich = document.getElementById('embed-rich');

    function openPanel() {
        panel!.hidden = false;
        toggle!.setAttribute('aria-expanded', 'true');
        document.body.style.overflow = 'hidden';
    }

    function closePanel() {
        panel!.hidden = true;
        toggle!.setAttribute('aria-expanded', 'false');
        document.body.style.overflow = '';
    }

    toggle?.addEventListener('click', () => {
        const isExpanded = toggle.getAttribute('aria-expanded') === 'true';
        if (isExpanded) closePanel();
        else openPanel();
    });

    backdrop?.addEventListener('click', closePanel);
    closeBtn?.addEventListener('click', closePanel);

    // Close on escape
    document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && !panel?.hidden) {
            closePanel();
        }
    });

    // Tab switching for embed styles
    embedTabs.forEach(tab => {
        tab.addEventListener('click', () => {
            const tabType = tab.getAttribute('data-tab');
            embedTabs.forEach(t => t.classList.remove('active'));
            tab.classList.add('active');

            if (tabType === 'simple') {
                embedSimple!.hidden = false;
                embedRich!.hidden = true;
            } else {
                embedSimple!.hidden = true;
                embedRich!.hidden = false;
            }
        });
    });

    // Copy functionality
    copyBtns.forEach(btn => {
        btn.addEventListener('click', async () => {
            const targetId = btn.getAttribute('data-copy');
            const target = document.getElementById(targetId!) as HTMLInputElement | HTMLTextAreaElement;

            if (target) {
                try {
                    await navigator.clipboard.writeText(target.value);
                    toast!.classList.add('show');
                    setTimeout(() => toast!.classList.remove('show'), 2000);
                } catch {
                    // Fallback for older browsers
                    target.select();
                    document.execCommand('copy');
                    toast!.classList.add('show');
                    setTimeout(() => toast!.classList.remove('show'), 2000);
                }
            }
        });
    });
</script>

<style>
    .embed-widget {
        position: relative;
    }

    .embed-toggle {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.6rem 1rem;
        background: var(--paper-3);
        border: 1px solid var(--rule);
        border-radius: var(--r-3);
        font-size: 0.85rem;
        font-weight: 500;
        color: var(--ink-2);
        cursor: pointer;
        transition: all 0.2s;
    }

    .embed-toggle:hover {
        background: var(--paper-3);
        color: var(--ink);
    }

    .embed-toggle svg {
        opacity: 0.7;
    }

    .embed-panel {
        position: fixed;
        inset: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 99999;
        padding: 1rem;
    }

    .embed-panel[hidden] {
        display: none;
    }

    .embed-backdrop {
        position: absolute;
        inset: 0;
        background: rgba(0, 0, 0, 0.6);
        animation: embedFadeIn 0.15s;
    }

    @keyframes embedFadeIn {
        from { opacity: 0; }
    }

    .embed-dialog {
        position: relative;
        width: min(440px, calc(100vw - 2rem));
        max-height: 85vh;
        overflow-y: auto;
        background: var(--paper-2);
        border: 1px solid var(--rule);
        border-radius: var(--r-4);
        padding: 1.25rem;
        box-shadow: var(--shadow-3);
        animation: embedSlideIn 0.2s cubic-bezier(0.16, 1, 0.3, 1);
    }

    @keyframes embedSlideIn {
        from {
            opacity: 0;
            transform: scale(0.96) translateY(8px);
        }
    }

    .embed-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1.25rem;
        padding-bottom: 0.75rem;
        border-bottom: 1px solid var(--rule);
    }

    .embed-header h4 {
        font-size: 0.95rem;
        font-weight: 600;
        color: var(--ink);
        margin: 0;
    }

    .close-btn {
        width: 28px;
        height: 28px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: var(--paper-3);
        border: none;
        border-radius: 6px;
        font-size: 1.25rem;
        color: var(--ink-3);
        cursor: pointer;
        transition: all 0.2s;
    }

    .close-btn:hover {
        background: var(--paper-3);
        color: var(--ink);
    }

    .embed-section {
        margin-bottom: 1.25rem;
    }

    .embed-section:last-child {
        margin-bottom: 0;
    }

    .embed-section label {
        display: block;
        font-size: 0.75rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        color: var(--ink-3);
        margin-bottom: 0.5rem;
    }

    .embed-hint {
        font-size: 0.8rem;
        color: var(--ink-3);
        margin: 0 0 0.5rem;
    }

    .input-group {
        display: flex;
        gap: 0.5rem;
    }

    .input-group input,
    .input-group textarea {
        flex: 1;
        min-width: 0;
        padding: 0.6rem 0.75rem;
        background: var(--paper);
        border: 1px solid var(--rule);
        border-radius: var(--r-3);
        font-size: 0.8rem;
        font-family: var(--font-mono);
        color: var(--ink-2);
    }

    .input-group textarea {
        font-size: 0.7rem;
        line-height: 1.4;
        word-break: break-all;
        white-space: pre-wrap;
        overflow-wrap: break-word;
        resize: vertical;
        max-height: 200px;
    }

    .copy-btn {
        display: flex;
        align-items: center;
        justify-content: center;
        width: 36px;
        background: var(--paper-3);
        border: 1px solid var(--rule);
        border-radius: var(--r-3);
        color: var(--ink-blue);
        cursor: pointer;
        transition: all 0.2s;
    }

    .copy-btn:hover {
        background: var(--paper-3);
    }

    /* Share Buttons */
    .share-buttons {
        display: flex;
        gap: 0.5rem;
    }

    .share-btn {
        width: 40px;
        height: 40px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: var(--paper-3);
        border: 1px solid var(--rule);
        border-radius: var(--r-3);
        font-size: 1rem;
        font-weight: 500;
        color: var(--ink-2);
        text-decoration: none;
        transition: all 0.2s;
    }

    .share-btn:hover {
        transform: translateY(-2px);
    }

    .share-btn.twitter:hover {
        background: #1da1f2;
        border-color: #1da1f2;
        color: var(--paper);
    }

    .share-btn.linkedin:hover {
        background: #0077b5;
        border-color: #0077b5;
        color: var(--paper);
    }

    .share-btn.email:hover {
        background: var(--verdant);
        border-color: var(--verdant);
        color: var(--paper);
    }

    .share-btn.whatsapp:hover {
        background: #25d366;
        border-color: #25d366;
        color: var(--paper);
    }

    /* Embed Tabs */
    .embed-tabs {
        display: flex;
        gap: 0.5rem;
    }

    .embed-tab {
        flex: 1;
        padding: 0.6rem 1rem;
        background: var(--paper-2);
        border: 1px solid var(--rule);
        border-radius: var(--r-3);
        font-size: 0.8rem;
        font-weight: 500;
        color: var(--ink-3);
        cursor: pointer;
        transition: all 0.2s;
    }

    .embed-tab:hover {
        background: var(--paper-3);
    }

    .embed-tab.active {
        background: var(--paper-3);
        border-color: var(--rule-strong);
        color: var(--ink-blue);
    }

    /* Preview */
    .embed-preview {
        margin-top: 0.75rem;
    }

    .preview-label {
        font-size: 0.7rem;
        color: var(--ink-3);
        display: block;
        margin-bottom: 0.4rem;
    }

    .preview-badge {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        padding: 8px 16px;
        background: var(--paper-2);
        border: 1px solid var(--rule);
        border-radius: var(--r-3);
        font-size: 0.85rem;
        color: var(--ink);
    }

    .badge-dot {
        color: var(--tier-color);
    }

    /* Rich Preview Card */
    .preview-card {
        display: inline-block;
        background: var(--paper-2);
        border: 1px solid color-mix(in srgb, var(--tier-color) 25%, transparent);
        border-radius: var(--r-4);
        padding: 12px 16px;
        min-width: 220px;
    }

    .preview-card__header {
        display: flex;
        align-items: center;
        gap: 8px;
        margin-bottom: 8px;
    }

    .preview-card__dot {
        width: 10px;
        height: 10px;
        border-radius: 50%;
        background: var(--tier-color);
    }

    .preview-card__name {
        color: var(--ink);
        font-weight: 500;
        font-size: 0.9rem;
    }

    .preview-card__specialty {
        color: var(--ink-3);
        font-size: 0.7rem;
        margin-bottom: 10px;
    }

    .preview-card__metrics {
        display: flex;
        gap: 12px;
    }

    .preview-metric {
        text-align: center;
    }

    .preview-metric__value {
        display: block;
        font-weight: 500;
        font-size: 1rem;
    }

    .preview-metric__label {
        font-size: 0.55rem;
        text-transform: uppercase;
        color: var(--ink-3);
    }

    .preview-card__footer {
        margin-top: 10px;
        padding-top: 6px;
        border-top: 1px solid var(--rule);
        font-size: 0.6rem;
        color: var(--ink-3);
    }

    /* Recommend Section */
    .recommend-section {
        padding-top: 1rem;
        border-top: 1px solid var(--rule);
    }

    .recommend-btn {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.6rem 1rem;
        background: var(--paper-3);
        border: 1px solid var(--rule);
        border-radius: var(--r-3);
        font-size: 0.85rem;
        font-weight: 500;
        color: var(--verdant);
        text-decoration: none;
        transition: all 0.2s;
    }

    .recommend-btn:hover {
        background: var(--paper-3);
    }

    /* Copied Toast */
    .copied-toast {
        position: fixed;
        bottom: 2rem;
        left: 50%;
        transform: translateX(-50%);
        padding: 0.5rem 1rem;
        background: var(--verdant);
        border-radius: var(--r-3);
        font-size: 0.8rem;
        font-weight: 600;
        color: var(--paper);
        opacity: 0;
        visibility: hidden;
        transition: all 0.2s;
    }

    .copied-toast.show {
        opacity: 1;
        visibility: visible;
    }

    /* Responsive */
    @media (max-width: 480px) {
        .embed-panel {
            align-items: flex-end;
            padding: 0;
        }

        .embed-dialog {
            width: 100%;
            border-radius: var(--r-4) var(--r-4) 0 0;
            max-height: 85vh;
        }
    }
</style>
